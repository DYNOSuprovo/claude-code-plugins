{
  "description": "Captures the plan of a branch and attaches it to the pull request that lands it",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"${CLAUDE_PLUGIN_ROOT}/scripts/capture-plan.ts\"",
            "timeout": 10,
            "statusMessage": "Capturing the plan..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"${CLAUDE_PLUGIN_ROOT}/scripts/attach-plan.ts\"",
            "timeout": 30,
            "statusMessage": "Attaching the plan to the PR..."
          }
        ]
      }
    ]
  }
}
