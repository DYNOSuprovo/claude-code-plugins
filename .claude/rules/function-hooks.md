---
paths:
  - "**/claude-code.d.ts"
---

# Function hooks

The generated types say what `$` and `on` accept; `docs/function-hooks-cheat-sheet.md` says what they mean: the tiers, the failure rules, which events carry a side effect, how `ui.render` draws.
