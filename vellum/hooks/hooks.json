{
  "description": "Holds the vellum planning mode: locks writes outside the working directory, serves the submit tool, closes on the stop skill, and delivers the review as a prompt",
  "modules": ["./register.ts"]
}
